#include<stdio.h>

int main(){
	int n,max=0,rank=0;;
	int arr[101];

	for(int i =1;i<=9;i++){
		scanf("%d",&arr[i]);
	}
	
	for(int i=1;i<=9;i++){
		if(arr[i]>max){
			max = arr[i];
			rank=i;
		}
	}
	
	printf("%d\n",max);
	printf("%d",rank);
	
}
